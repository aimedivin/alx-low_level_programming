#include <stdio.h>

/**
 * main - prints its name, followed by a new line
 * @argc: Thhis in the number of command line argument
 * @argv: This is an array name
 * Return: 0
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[argc * 0]);

	return (0);
}
